apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: {{ .Values.kong.ratelimiting }}           # ex. products-api-key-auth, templatisé
  namespace: {{ .Values.namespace }}      # ex. ncap
plugin: rate-limiting
config:
  minute: 5
  policy: local
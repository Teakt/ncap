apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
  annotations:
      konghq.com/plugins: {{ .Values.kong.keyauth }}, {{ .Values.kong.ratelimiting }}
spec:
  ingressClassName: kong
  rules:
  - http:
      paths:
      - path: /products
        pathType: Prefix
        backend:
          service:
            name: products-api
            port:
              number: 80

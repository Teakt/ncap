apiVersion: v1
kind: Service
metadata:
  name: products-api
  namespace: ncap
spec:
  selector:
    app: products-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000